numpy:
  - 1.16 # [unix]

python:
  - 3.8

mpi:
#  - openmpi #   [osx]
  - mpich
#  - mpich2  #   [linux]

pin_run_as_build:
    openmpi:
      max_pin: x.x
    mpich:
      max_pin: x.x
    mpich2:
      max_pin: x.x

