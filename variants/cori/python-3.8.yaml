numpy:
  - 1.16 # [unix]

python:
  - 3.8

crayhost:
  - cori

mpi:
  - cori.prgenv.gnu.mpi

c_compiler :
  - cori.prgenv.gnu
cxx_compiler :
  - cori.prgenv.gnu
fortran_compiler :
  - cori.prgenv.gnu

pin_run_as_build:
    cori.prgenv.gnu.mpi:
      max_pin: x.x.x
    cori.prgenv.gnu_linux64:
      max_pin: x.x.x
